# End of life — camera-only front

`Front-Camera.stl` is **not** the current release. It is kept here for existing prints and archives.

The current camera front includes the sound-detector pocket. Print from `camera/v1/`:

- `Front-Camera-Sound-Detector.stl`
- `Sound-Detector-Retainer.stl`
