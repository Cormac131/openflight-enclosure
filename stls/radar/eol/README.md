# End of life — filled radar front

`Front-Radar.stl` (cover in front of the radars) is **not** the current release. It is kept here for existing prints and archives.

The cover blocks RF and IWR flashing after the module is mounted. Print the current no-fill front from `radar/v1/`:

- `Front-Radar-No-Fill.stl`
